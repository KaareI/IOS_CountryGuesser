import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        backgroundColor: '#252629',
        paddingVertical: 20,
        marginBottom: 20,
        borderRadius: 8,
        width: '100%'
    },
    title: {
        color: '#FFFFFF',
        textAlign: "center",
        fontSize: 20,
        fontWeight: "bold",
    }
})